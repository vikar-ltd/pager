export default function HomePage() {
  return (
    <div>
      <h1>Home</h1>
      <p>Welcome to the demo shop. Try the nav — each Link click should log a pageview via the tracker.</p>
      <p>Then visit <code>/signup</code> and click the CTA to fire a custom event.</p>
    </div>
  );
}
